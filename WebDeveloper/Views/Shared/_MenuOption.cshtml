<ul class="nav navbar-nav">
    <li>@Html.ActionLink("Home", "Index", "Home")</li>
    @if(Request.IsAuthenticated)
    { 
        if (User.IsInRole("Admin"))
        {
            <li>@Html.ActionLink("Intranet", "Index","Intranet",null, new { area="Internal"})</li>
        }
        <li>@Html.ActionLink("Autor", "Index", "Autor", null, new { area= "Autor" })</li>    
    }
</ul>

